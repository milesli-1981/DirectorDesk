{
  "name": "directdesk-previs-mcp",
  "version": "0.1.0",
  "description": "MCP server: render a DirectorState scene JSON into a previs video via the Director Desk web app (headless).",
  "type": "module",
  "bin": {
    "directdesk-previs": "index.mjs"
  },
  "scripts": {
    "start": "node index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.0",
    "playwright": "^1.49.0",
    "zod": "^3.23.8"
  }
}
